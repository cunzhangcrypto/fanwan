# 饭碗儿环境变量说明
#
# 复制本文件为 .dev.vars 用于本地开发（wrangler dev 会自动读取）；
# 部署到 Cloudflare 时，三个 SECRET 必须用 wrangler secret put 设置，不要放配置文件里。
# 公开值（TURNSTILE_SITE_KEY 等）配置在 wrangler.toml 的 [vars] 里。

# Cloudflare Turnstile 密钥（控制台 https://dash.cloudflare.com → Turnstile 创建站点）
# TURNSTILE_SECRET_KEY 必须通过 wrangler secret put TURNSTILE_SECRET_KEY 设置
TURNSTILE_SECRET_KEY=

# IP 日限哈希用的服务端密钥（随便生成一串随机字符）
# wrangler secret put SERVER_SECRET
SERVER_SECRET=

# 饭碗儿后台管理密钥（Bearer Token）
# wrangler secret put ADMIN_KEY
ADMIN_KEY=

# —— 留言通知（可选，不配置就不推）——
# Telegram Bot Token：找 @BotFather 建一个机器人拿 token（碗主人自己填 chat_id）
# wrangler secret put TELEGRAM_BOT_TOKEN
TELEGRAM_BOT_TOKEN=

# 邮箱通知：不需要平台配任何邮箱密钥。
# 邮件由碗主人自己在「编辑碗 → 留言提醒」里填（邮件 API 地址 + 自己的 API Key + 发件人），
# 平台只负责 fetch 调碗主人的接口发信，消耗的是碗主人的额度，跟平台莫得关系。
